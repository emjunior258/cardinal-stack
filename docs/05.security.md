# Security

## Access Management

Cardinal Stack implements a Role-Based Access Control (RBAC) model for the Administration and Operations APIs. The model is designed around three core principles:

- **Identity is owned by the Identity Provider.** The platform trusts JWTs issued by the configured IdP and never manages credentials.
- **Authorization is owned by the platform.** Role assignments are managed exclusively via the Administration API. The IdP has no knowledge of tenant or application-level roles.
- **Least privilege by design.** Roles are narrowly scoped. Configuration, operations, and access management are separated into distinct roles at every level.

### Principals

A principal is an entity to which a role can be assigned. The platform recognises three principal types, each identified by a claim in the JWT:

| Type | JWT Claim | Description |
|---|---|---|
| `user` | `sub` | A human user authenticated via Authorization Code flow |
| `group` | `groups` | An Identity Provider group. Matches any entry in the `groups` claim array |
| `client` | `client_id` | A machine-to-machine client authenticated via Client Credentials flow |

### Role Assignments

A role assignment is a record of the form `(principal, role, scope)`.

**Scopes** define the boundary within which a role is effective:

| Scope | Description |
|---|---|
| `platform` | Applies across the entire platform |
| `space(spaceId)` | Applies within a specific space |
| `application(spaceId, appId)` | Applies within a specific application |

**Conflict resolution:** When multiple assignments match the same principal within the same scope, the highest privilege role takes effect.

**Scope boundary:** A role assignment never grants access beyond its defined scope. A space-scoped assignment grants no visibility into sibling spaces. An application-scoped assignment grants no visibility into sibling applications within the same space.

### Root Access

Root is a special access level that bypasses all permission checks. It exists exclusively to bootstrap the platform — whoever holds Root assigns the first Platform Access Admin and Platform Configurator, then steps back.

Root is not a role that can be assigned via the Administration API. It is configured exclusively via the platform configuration file:

```yaml
security:
  access_management:
    root_access:
      subs:        ["..."]   # matched on `sub` claim
      groups:      ["..."]   # matched on `groups` claim
      client_ids:  ["..."]   # matched on `client_id` claim
```

Any combination of `subs`, `groups`, and `client_ids` may be specified. Multiple principals can hold Root simultaneously.

> **Warning:** The platform emits a startup warning if Root principals are configured when running in production mode. Root access should be removed from the configuration once the initial Platform Access Admin and Platform Configurator assignments have been made.

### Environment-Conditional Permissions

Certain permissions are restricted to non-production deployments. When the platform is running in production mode, Space Developers and Application Developers lose access to Debug Probes and Session Recordings regardless of their role assignment. This protects real subscriber data from being exposed to developers in production environments.

---

## Roles

Roles are organised into three levels that mirror the platform's resource hierarchy: platform, space, and application.

### Platform-Level Roles

Platform-level roles are assigned at platform scope and apply across the entire platform.

| Role | Description |
|---|---|
| **Platform Configurator** | Manages platform infrastructure: networks, short codes, spaces, space entitlements, and global response templates and error mappings. Has no access management privileges. |
| **Platform Access Admin** | Manages role assignments at platform scope and space scope. Has no resource configuration privileges. |
| **Platform Operator** | Troubleshoots issues across all spaces and applications. Has CRUD access to short lists and full access to the Operations API across all spaces. |
| **Platform Config Viewer** | Read-only access to all administration API resources across all spaces. No Operations API access. |

### Space-Level Roles

Space-level roles are assigned at space scope and apply within a specific space.

| Role | Description |
|---|---|
| **Space Configurator** | Full configuration control within the space: key pairs, certificate issuers, short lists, response templates, error mappings, applications, backends, entry points, and session tag rules. Has no access management privileges. |
| **Space Access Admin** | Manages role assignments within the space and its applications. Has no resource configuration privileges. |
| **Space Developer** | Manages applications and all application sub-resources within the space. Has no access to the Operations API. In non-production environments, can create debug probes and view session recordings. |
| **Space Operator** | Read-only access to all space configuration resources. Full Operations API access for the space including cache invalidation and runtime short list management. |
| **Space Viewer** | Read-only access to all space resources. |
| **Short List Manager** | Creates and manages short lists within the space. Manages short lists at runtime via the Operations API. Designed for delegation to non-engineering teams such as marketing or operations. |

### Application-Level Roles

Application-level roles are assigned at application scope and apply within a specific application. Application-level roles have no visibility into sibling applications within the same space.

| Role | Description |
|---|---|
| **Application Developer** | Full configuration of the application: backends, entry points, and session tag rules. Read access to space short lists for reference. Has no access to the Operations API. In non-production environments, can create debug probes and view session recordings. |
| **Application Operator** | Full Operations API access for the application. Read-only access to application configuration. |
| **Application Viewer** | Read-only access to application configuration and non-sensitive Operations API data. |

---

## Role Assignment Permissions

The ability to assign roles is itself governed by role. The table below defines which roles can assign which other roles and within which scope.

| Assignor | Assignable Roles | Scope |
|---|---|---|
| **Root** | Any role | Any scope |
| **Platform Access Admin** | Platform Access Admin, Platform Operator, Platform Config Viewer, Space Access Admin, Space Configurator, Space Developer, Space Operator, Space Viewer, Short List Manager, Application Developer, Application Operator, Application Viewer | Platform + all spaces + all applications |
| **Space Access Admin** | Space Access Admin, Space Configurator, Space Developer, Space Operator, Space Viewer, Short List Manager, Application Developer, Application Operator, Application Viewer | Own space + its applications |

**Key constraints:**

- Only Root can assign Platform Configurator
- Only Root can assign or revoke Root
- Platform Access Admin cannot assign Root or Platform Configurator
- Space Access Admin cannot assign any platform-level roles
- All assignments are strictly contained within the assignor's scope

---

## Permission Reference

The following tables define the complete set of permissions for each role across the Administration API and Operations API.

### Administration API

#### Platform-Level Roles

| Resource | Root | Plat Configurator | Plat Access Admin | Plat Operator | Plat Config Viewer |
|---|---|---|---|---|---|
| Networks | ALL | CRUD | — | R | R |
| Short Codes | ALL | CRUD | — | R | R |
| Spaces | ALL | CRUD | — | R | R |
| Space Entitlements | ALL | CRUD | — | R | R |
| Global Response Templates | ALL | CRUD | — | R | R |
| Global Error Mappings | ALL | CRUD | — | R | R |
| Key Pairs | ALL | — | — | R | R |
| Certificate Issuers | ALL | — | — | R | R |
| Short Lists | ALL | — | — | CRUD | R |
| Space Response Templates | ALL | — | — | R | R |
| Space Error Mappings | ALL | — | — | R | R |
| Applications | ALL | — | — | R | R |
| Backends | ALL | — | — | R | R |
| Entry Points | ALL | — | — | R | R |
| Session Tag Rules | ALL | — | — | R | R |
| Cache Invalidation | ALL | — | — | — | — |
| Role Assignments | ALL | — | CRUD | — | — |

#### Space-Level Roles

| Resource | Root | Space Configurator | Space Access Admin | Space Developer | Space Operator | Space Viewer | Short List Manager |
|---|---|---|---|---|---|---|---|
| Space (own) | ALL | R | — | R | R | R | — |
| Space Entitlements | ALL | R | — | R | — | R | — |
| Key Pairs | ALL | CRUD | — | LIST | R | R | — |
| Certificate Issuers | ALL | CRUD | — | R | R | R | — |
| Short Lists | ALL | CRUD | — | R | R | R | CRUD |
| Space Response Templates | ALL | CRUD | — | R | R | R | — |
| Space Error Mappings | ALL | CRUD | — | R | R | R | — |
| Applications | ALL | CRUD | — | CRUD | R | R | — |
| Backends | ALL | CRUD | — | CRUD | R | R | — |
| Entry Points | ALL | CRUD | — | CRUD | R | R | — |
| Session Tag Rules | ALL | CRUD | — | CRUD | R | R | — |
| Cache Invalidation | ALL | EXEC | — | EXEC | EXEC | — | — |
| Role Assignments | ALL | — | CRUD | — | — | — | — |

#### Application-Level Roles

| Resource | Root | Application Developer | Application Operator | Application Viewer |
|---|---|---|---|---|
| Application (own) | ALL | R | R | R |
| Backends | ALL | CRUD | R | R |
| Entry Points | ALL | CRUD | R | R |
| Session Tag Rules | ALL | CRUD | R | R |
| Short Lists (space) | ALL | R | — | — |
| Cache Invalidation | ALL | EXEC | — | — |
| Role Assignments | ALL | — | — | — |

---

### Operations API

#### Platform-Level Roles

| Resource | Root | Plat Configurator | Plat Access Admin | Plat Operator | Plat Config Viewer |
|---|---|---|---|---|---|
| Active Sessions | ALL | — | — | R | — |
| Session State | ALL | — | — | R | — |
| Session Content Stream | ALL | — | — | R | — |
| Debug Probes | ALL | — | — | CRUD | — |
| Session Recordings | ALL | — | — | R | — |
| Short Lists (runtime) | ALL | — | — | CRUD | — |

#### Space-Level Roles

| Resource | Root | Space Configurator | Space Access Admin | Space Developer | Space Operator | Space Viewer | Short List Manager |
|---|---|---|---|---|---|---|---|
| Active Sessions | ALL | R | — | — | R | R | — |
| Session State | ALL | R | — | — | R | R | — |
| Session Content Stream | ALL | R | — | — | R | — | — |
| Debug Probes | ALL | CRUD | — | CRUD † | CRUD | R | — |
| Session Recordings | ALL | R | — | R † | R | — | — |
| Short Lists (runtime) | ALL | CRUD | — | — | CRUD | — | CRUD |

† Non-production environments only

#### Application-Level Roles

| Resource | Root | Application Developer | Application Operator | Application Viewer |
|---|---|---|---|---|
| Active Sessions | ALL | — | R | R |
| Session State | ALL | — | R | R |
| Session Content Stream | ALL | — | R | — |
| Debug Probes | ALL | CRUD † | CRUD | R |
| Session Recordings | ALL | R † | R | — |
| Short Lists (runtime) | ALL | — | — | — |

† Non-production environments only

---

> Sessions are created and terminated exclusively by the platform lifecycle. No role can create, update, or terminate a session via the API.

---

## Audit Logging

Cardinal Stack records a tamper-evident audit trail of sensitive operations. The audit log allows operators, access admins, and security teams to answer the question: *who did what, to which resource, and when?*

### What is logged

Two categories of operation produce audit records:

**Modificative operations** — any write action on the Administration API:

- Create, update, or delete: Networks, Short Codes, Spaces, Space Entitlements, Key Pairs, Certificate Issuers, Applications, Backends, Entry Points, Session Tag Rules, Short Lists, Response Templates, Error Mappings
- Role assignment create, update, or delete
- Cache invalidation (execute)

**Privacy-sensitive read operations** — Operations API access that exposes subscriber data:

- Session state inspection
- Session content stream access
- Session recording access
- Debug probe creation and access

**System operations:**

- Root access usage — every request authenticated as a Root principal produces an audit record regardless of the operation performed
- Audit log retention cleanup execution (see [Retention](#retention))

### Audit event schema

| Field | Type | Description |
|---|---|---|
| `id` | string (UUID) | Unique identifier for this audit event |
| `occurredAt` | string (ISO 8601) | Timestamp when the operation occurred |
| `actor.type` | enum | `user`, `group`, `client`, or `system` |
| `actor.identifier` | string | The JWT claim value that identifies the actor: `sub` for users, group name for groups, `client_id` for clients. Empty for `system` actors. |
| `actor.ip` | string | Source IP address of the request. Absent for `system` actors. |
| `actor.isRoot` | boolean | `true` if the actor exercised Root access for this operation |
| `operation` | enum | `create`, `read`, `update`, `delete`, `execute`, `assign`, `revoke`, `cleanup` |
| `resourceType` | string | The type of resource affected (e.g. `space`, `application`, `role_assignment`, `session_state`, `session_recording`) |
| `resourceId` | string | Identifier of the affected resource. Absent for cleanup events. |
| `affectedResourcesCount` | number | (optional) Number of resources affected by the operation. Present for bulk operations such as cleanup. Absent for single-resource operations. |
| `scope` | string | `platform`, `space:<id>`, or `application:<spaceId>:<appId>` |
| `outcome` | enum | `success`, `denied`, `failure` |
| `failureReason` | string | (optional) Human-readable reason; present when `outcome` is `denied` or `failure` |
| `requestId` | string | Correlates this record with the distributed trace for the same request. Absent for `system` actors. |

`denied` outcomes — authorization failures — are recorded with the same fidelity as successful operations. Repeated denial patterns are as operationally significant as successful access.

### Retention

Audit records are retained for a configurable period, after which they are permanently deleted. The retention period is defined in the platform configuration file:

```yaml
security:
  audit_log:
    retention_days: 365   # default: 365
```

A background process runs daily and deletes all `audit_log` records older than the configured retention period. The cleanup itself emits an audit event (`operation: cleanup`, `actor.type: system`) with `affectedResourcesCount` set to the number of records deleted. This event is written within the same database transaction as the deletion, ensuring the cleanup is always reflected in the audit trail.

### Access control

The **Auditor** role provides read-only access to audit log records within its assigned scope. Auditors cannot modify records or perform any other platform operation.

| Role | Scope | Visibility |
|---|---|---|
| **Platform Auditor** | Platform | All audit events across the entire platform |
| **Space Auditor** | Space | Audit events within the assigned space and its applications |

The Platform Auditor and Space Auditor roles are assignable by Platform Access Admin and Space Access Admin respectively, following the same delegation constraints defined in [Role Assignment Permissions](#role-assignment-permissions).

### Audit log API

```
GET /audit-log
```

Returns a paginated list of audit events visible to the caller. The response is always scoped to the principal's audit-visible boundary — a Space Auditor receives only events within their assigned space.

**Query parameters:**

| Parameter | Description |
|---|---|
| `from` | ISO 8601 timestamp — return events at or after this time |
| `to` | ISO 8601 timestamp — return events before or at this time |
| `actor` | Filter by actor identifier |
| `resourceType` | Filter by resource type |
| `operation` | Filter by operation |
| `outcome` | Filter by outcome (`success`, `denied`, `failure`) |
| `scope` | Filter by scope string |
| `pageToken` | Pagination cursor from a previous response |
| `pageSize` | Number of records per page (default: 50, max: 500) |

**Response:**

```json
{
  "events": [ ... ],
  "nextPageToken": "..."
}
```

`nextPageToken` is absent when there are no further pages.
